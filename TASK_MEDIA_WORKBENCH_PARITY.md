# 多媒体工作台功能对齐任务书

> 创建日期：2026-07-26  
> 目标：在不增加浏览器扩展、桌面/移动客户端和登录鉴权的前提下，补齐 DataTool 网站公开展示的核心媒体处理能力。  
> 生产目标：`https://video-collector.ximoai.cn`，唯一服务器 `47.251.87.147`。

## 1. 明确排除

- 不制作浏览器扩展。
- 不制作 Windows、macOS、Android 或 iOS 客户端。
- 不增加注册、登录、会员、Cookie 导入或第三方 SSO。
- 不复制 DataTool 的品牌、广告、博客、社区和企业内容。
- 不绕过 DRM、付费墙、登录限制、平台访问控制或版权水印。

## 2. 必须完成的站内能力

### 2.1 单条与批量媒体

- 单条公开 URL 解析与下载。
- 一次最多 10 条 URL 的批量解析；逐条显示成功或错误。
- 批量任务可以分别选择并下载，不允许无限队列。
- 公开创作者/播放列表入口最多读取前 10 条，不抓取完整账户历史。

### 2.2 视频与音频

- 展示源站提供的全部有效视频与纯音频格式。
- 视频支持分辨率、容器、大小和音轨状态选择。
- 无音轨视频自动与最佳公开音轨合并。
- 提供独立“提取 MP3”任务，服务端通过 FFmpeg 转换。
- 4K 仅在源站真实提供且解析器可访问时显示，不伪造画质。
- “无水印”含义限定为下载源站公开提供的无水印源文件；不做水印移除算法。

### 2.3 图片与图集

- 返回解析器提供的封面和图片候选，按分辨率去重。
- 支持下载单张图片。
- 图集/公开集合最多显示并下载前 10 张图片。
- 图片 URL 必须来自服务端重新解析的受信媒体结果，客户端不能提交任意代理 URL。

### 2.4 字幕

- 返回人工字幕与自动字幕的语言列表。
- 支持按语言提取字幕并统一输出 SRT。
- 字幕任务进入同一匿名任务队列、复用取消、进度、下载和 TTL。

### 2.5 AI 转录

- URL 模式：下载公开最佳音轨后离线转录。
- 文件模式：上传用户主动选择的音频或视频后离线转录。
- 输出 SRT 和文本预览；结果继续使用临时文件下载机制。
- 使用容器内离线多语言 Whisper 模型，不依赖登录或外部付费 API。
- 单文件最大 250 MiB，单次媒体最长 60 分钟，转录任务计入全局并发。

### 2.6 公开数据与导出

- 展示解析器公开返回的播放、点赞、评论、转发数量。
- 批量或创作者结果支持导出 Excel 可直接打开的 UTF-8 CSV。
- 不抓取需要登录的评论正文，不保存用户画像。

### 2.7 工作台界面

- 提供视频、音频、图片、字幕、AI 转录五个明确入口。
- 提供单条、批量、创作者三种 URL 输入方式。
- 提供全部、处理中、已完成、失败的当前会话任务视图。
- 保持主站中英语言协议、深浅主题、字体和主按钮样式同步。
- 继续显著显示：未下载 30 分钟删除，点击下载后 15 分钟删除。

## 3. 安全与资源限制

- 所有 URL 继续执行 HTTP/HTTPS、DNS、私网地址、云元数据和重定向安全检查。
- JSON 请求体最大 16 KiB；批量 URL 最多 10 条，每条最长 2048 字符。
- 上传只接受明确的音频/视频扩展和 MIME，最大 250 MiB。
- 文件名、格式 ID、字幕语言和任务类型使用允许列表。
- 外部进程全部使用参数数组，不经过 Shell。
- 匿名任务 ID 使用密码学随机值；不提供全局任务枚举 API。
- 解析、下载、转录共用 IP 限流、并发上限、有界队列和硬超时。
- 项目产生的缓存只写入项目根 `cache` 映射的 `/app/cache`。
- 活动下载结束前不删除文件；未领取 30 分钟、首次领取后 15 分钟删除。

## 4. API 验收契约

- `POST /api/v1/media/parse`：单条媒体、图片、字幕和公开指标。
- `POST /api/v1/media/batch`：最多 10 条，返回逐条结果。
- `POST /api/v1/collections/parse`：最多 10 条公开集合条目。
- `POST /api/v1/tasks`：媒体、MP3、图片、字幕、URL 转录任务。
- `POST /api/v1/transcriptions/upload`：受限 multipart 文件转录。
- `GET /api/v1/tasks/{id}`：进度、文件信息和可选文本预览。
- `DELETE /api/v1/tasks/{id}`：取消任务。
- `GET /api/v1/tasks/{id}/download`：浏览器原生下载。

## 5. 自动化验收

- [x] 音频格式解析和 MP3 转换测试。
- [x] 图片候选去重与受信下载测试。
- [x] 人工/自动字幕归一化和 SRT 输出测试。
- [x] 10 条批量上限、逐条错误和集合上限测试。
- [x] 公开指标归一化与 CSV 导出测试。
- [x] 上传类型、大小、路径和限流测试。
- [x] URL 与上传转录任务测试。
- [x] 全部 Go、Vitest、类型检查和生产构建通过。
- [x] Docker 镜像包含 yt-dlp、FFmpeg、Whisper CLI 和本地模型。

## 6. 生产验收

- 测试链接由实施过程自行寻找，必须覆盖公开视频、纯音频、图片/图集、字幕和公开创作者/集合；不得只使用模拟数据。
- 测试样本必须是无需登录、无需 Cookie、无 DRM、允许公开访问的内容。
- [x] 本地完整容器为 `healthy`，`/health` 返回完整运行时信息。
- [x] 视频下载与浏览器领取通过。
- [x] 纯音频或 MP3 下载通过。
- [x] 图片下载通过。
- [x] 字幕 SRT 下载通过。
- [x] URL 转录和文件转录至少各通过一个短样本。
- [x] 批量解析同时包含成功和失败项时结果正确。
- [x] 主站中英、深浅主题实时同步。
- [x] 390px 与桌面布局无横向溢出。
- [x] 显著清理说明和 15/30 分钟策略保持不变。

本地完整容器验收记录保存在项目缓存 `cache/acceptance/acceptance-results.json`，样本覆盖 AcFun、NASA SoundCloud、SoundCloud 公开集合和 TED。缓存与测试媒体不提交仓库。

保存逻辑是不可变约束：所有媒体、图片、字幕和转录结果均为服务器临时文件；未点击下载 30 分钟删除，首次点击下载后 15 分钟删除，活动下载结束前不删除。

只有自动化与生产验收完成后，才把本任务标记为完成。
