# Video Collector 公开平台适配与错误语义任务书

> 文档状态：执行中
> 创建日期：2026-07-26
> 唯一生产服务器：`47.251.87.147`
> 唯一正式地址：`https://video-collector.ximoai.cn`

## 1. 目标

在现有 yt-dlp、FFmpeg、图片下载和国内临时出口能力上，完成公开媒体平台适配层的下一轮修复，使系统能够：

1. 对 yt-dlp 已支持的公开、免登录内容继续提供视频、音频、图片和字幕能力。
2. 对受 IP 或地域影响且命中受控域名的请求，在美国直连失败后仅切换一次国内临时出口。
3. 当两条出口都失败时保留真实平台失败原因，不再统一丢失为“direct and alternate egress failed”。
4. 明确区分平台风控、需要登录或 Cookie、DRM/会员内容、不支持 URL、内容不存在和临时网络故障。
5. 让用户知道哪些问题可以稍后重试，哪些问题不在本项目能力范围内。

## 2. 固定边界

- 不做登录鉴权，不接收或保存用户账号、Cookie、验证码、Token 或浏览器会话。
- 不实现 DRM、付费墙、会员、私密内容或地区规则绕过。
- 不复制 DataTool 的访客指纹、私有加密协议或不可审计后端接口。
- 不添加公开代理池、住宅代理、请求头伪造轮换或 SSL Bump。
- 国内服务器只提供 `10.77.0.2:3128` 上的无缓存私有出口，不运行本项目、不保存媒体。
- 所有应用、任务、临时媒体和下载文件继续只位于美国服务器项目缓存。
- 未点击下载的任务 30 分钟后清理；首次点击下载后 15 分钟清理，逻辑保持不变。
- 主站 `D:\sub2api中转站` 只读，不在本任务中修改。

## 3. 根因结论

### 3.1 国内出口已经可用

WireGuard 与专用 Squid 的真实 PoC 已通过：

- 美国端 `10.77.0.1/30` 可访问国内端 `10.77.0.2/30`。
- `https://example.com/` 和 `https://www.baidu.com/` 经国内出口返回 200。
- 云元数据地址经代理返回 403。
- 公网 `47.99.92.130:3128` 不可访问。
- 两端默认路由、Docker 网络和原有容器未改变。

因此后续平台失败不能一律归因于代理或 WireGuard。

### 3.2 DataTool 的公开前端不是平台解析器

公开脚本显示 DataTool 前端只完成访客标识、提交 URL、解密后端结果和下载调度。真正的平台解析器位于其私有后端，公开代码不能证明其使用了何种平台接口、Cookie、IP 池或风控策略。本项目只复用可验证的产品能力概念，不复制其私有协议。

### 3.3 当前项目的共享根因

- yt-dlp 已经承担平台识别和绝大多数平台适配，不需要再平行维护一份庞大提取器注册表。
- 当前 `executeWithEgress` 在直连与国内出口都失败时丢弃第二次平台错误，导致上层无法给出真实原因。
- 当前 `extractorError` 只截取 yt-dlp 最后一行英文错误，没有稳定、可操作的失败语义。
- 生产出口示例只包含 `bilibili.com,b23.tv`，尚未包含已经通过代理验证的 `acfun.cn`。

## 4. 平台验收矩阵

| 平台 | 公开样本 | yt-dlp 官方状态 | 当前实测 | 本轮结论 |
| --- | --- | --- | --- | --- |
| AcFun | `https://m.acfun.cn/v/?ac=15974291` | `AcFunVideo` | 国内出口解析成功 | 纳入受控出口规则并做真实解析/下载回归 |
| Bilibili | `https://www.bilibili.com/video/BV1MApNzKE84/` | `BiliBili` | 美国直连和国内出口均 HTTP 412 | 归类为平台风控；换官方公开样本复测，不引入 Cookie 或指纹绕过 |
| 西瓜视频 | `https://m.ixigua.com/video/6812431579554513408` | `Ixigua` | 要求 Cookie | 明确提示需要 Cookie，本项目不处理 |
| 好看视频 | 已测试页面 | 通用提取器 | `Unsupported URL` | 明确提示当前解析器不支持 |
| 腾讯视频 | `https://v.qq.com/x/page/w0647n5294g.html` | 官方支持清单无专用提取器 | 待真实执行 | 只验证通用提取器，不承诺平台支持 |

yt-dlp 官方也明确说明：支持清单不是可用性保证，网站变化可能随时破坏提取器，唯一可靠方法是真实测试。

## 5. 最小实现方案

### 5.1 保留 yt-dlp 作为通用平台适配器

不新增一套自研解析器框架。现有 `extractor` 字段继续作为解析成功后的平台标识，视频、音频、图片、字幕和转录继续复用同一组元数据与下载入口。

只有在同时满足以下条件时，未来才新增平台专用适配器：

1. 平台有公开、免登录、无需 Cookie 且允许服务器调用的稳定接口。
2. 可以用自动化测试固定输入输出。
3. 不需要模仿浏览器指纹或规避访问控制。
4. 通用 yt-dlp 提取器无法提供同等能力。

### 5.2 保留双出口后的真实错误

调整 `executeWithEgress`：

- 第一次失败仍按既有规则决定是否切换出口。
- 第二次失败时返回第二次请求的真实 `stderr` 和错误。
- 不返回代理 URL、密钥、内部地址或完整目标 URL。
- 继续限制最多切换一次，清理失败下载片段，保持熔断和路由 TTL。

### 5.3 统一公开错误语义

在 `extractorError` 的共享出口映射以下类别：

- `platform_restricted`：HTTP 412、CAPTCHA、平台风控或 IP 拒绝。
- `authentication_required`：登录、Cookie、账号或会员权限。
- `drm_protected`：DRM 或受保护媒体。
- `unsupported_url`：yt-dlp 不支持该链接或页面类型。
- `media_unavailable`：404、删除、私密或不可公开访问。
- `temporary_failure`：429、5xx、超时、DNS 或连接重置。
- `unknown`：其余已清洗、已限长的提取器错误。

公开消息必须说明下一步，但不得建议提交 Cookie、账号或绕过平台限制。

### 5.4 AcFun 出口规则

将生产启用示例更新为：

```text
VIDEO_COLLECTOR_EGRESS_MODE=auto
VIDEO_COLLECTOR_CN_PROXY_URL=http://10.77.0.2:3128
VIDEO_COLLECTOR_CN_PROXY_SOURCE_HOSTS=bilibili.com,b23.tv,acfun.cn
```

仍保持 `.env.example` 默认 `off`；只有真实验收全部通过后才在生产 `.env` 启用。

## 6. TDD 实施顺序

1. 为“双出口失败保留真实平台错误”添加失败测试。
2. 为 412、Cookie、DRM、不支持 URL、404/删除和临时故障添加错误分类测试。
3. 为 AcFun 域名出现在部署启用示例中添加配置测试。
4. 运行失败测试，确认测试确实覆盖当前缺陷。
5. 实现最小修复并运行 Go、前端及 race 测试。
6. 构建完整生产镜像后，在美国服务器用容器内 yt-dlp 和正式 API 分别验收。

## 7. 验收标准

### 7.1 自动化

- [ ] 双出口都返回 HTTP 412 时，上层仍收到平台风控类错误，而不是通用出口失败。
- [ ] Cookie/login/DRM 错误不会触发国内出口回退。
- [ ] `Unsupported URL` 不会触发国内出口回退。
- [ ] HTTP 403/408/412/429/451、超时和地域错误仍允许受控域名切换一次出口。
- [ ] AcFun 域名只作为服务端受控规则出现，前端没有代理输入框。
- [ ] `go test ./...`、`go test -race ./...`、`npm test`、`npm run typecheck`、`npm run build:web` 全部通过。

### 7.2 真实平台

- [ ] AcFun：正式 API 解析成功，至少一个视频格式可由浏览器下载，下载响应为附件且非 HTML 错误页。
- [ ] Bilibili：使用仍可在普通浏览器匿名播放的官方公开样本复测；成功则验证下载，失败则页面显示明确平台风控提示。
- [ ] 腾讯视频：执行一次通用提取器验证；若 `Unsupported URL`，记录为官方不支持，不引入私有解析器。
- [ ] 西瓜视频：页面显示需要 Cookie/登录且本项目不处理的明确提示。
- [ ] 好看视频：页面显示当前链接类型不受支持的明确提示。
- [ ] `/health` 保持 `status=ok`，启用后 `egressStatus` 不为 `off`。
- [ ] 美国服务器默认路由、生产域名和浏览器下载行为不变。

## 8. 部署门槛与回滚

在完成自动化、镜像、真实解析和浏览器下载验收前：

- 两端 WireGuard 与国内专用 Squid 可以保持 `active`，但不得设置为开机自动启动。
- 生产应用继续保持 `VIDEO_COLLECTOR_EGRESS_MODE=off`。

完整验收后再决定是否启用 systemd 开机启动和应用 `auto` 模式。应用侧快速回滚只需将 `VIDEO_COLLECTOR_EGRESS_MODE=off` 后重建容器；网络侧回滚分别停止专用 Squid 和 `wg-quick@wg-vc-egress.service`，不修改默认路由和现有项目。
