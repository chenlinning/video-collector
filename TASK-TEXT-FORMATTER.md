# 文本格式化与按行限字分段任务书

> 状态：核心功能、紧凑工具栏与分段选择复制增强均已部署生产并通过线上验收
> 创建日期：2026-07-28
> 完成日期：2026-07-28
> 目标：在现有 Video Collector 网站中新增独立的“文本格式化”功能，并记录实现进度与验收结果。

## 1. 目标

新增一个文本格式化工具，让用户能够输入文章或导入文本文件，完整提取可读取的文字，将标点符号替换为换行，并在不截断任何完整行的前提下，按用户设定的最大字数导出分段结果。

核心规则：

1. 文字内容不得丢失、重复或改变顺序。
2. 只移除标点符号，不移除文字、数字、英文或其他非标点内容。
3. 原文已有换行必须保留。
4. 每组连续标点符号产生一个换行。
5. 原文换行与标点产生的换行连续出现时，最终只保留一个换行。
6. 分段只能发生在换行边界。
7. 加入下一整行将超过字数上限时，在该行之前结束当前分段，并让该整行成为下一分段的开始。
8. 每个正常分段不得超过用户设定的最大字数，不能在一行中间截断。

## 2. 明确范围

### 2.1 输入方式

- 直接输入或粘贴文章。
- 从本地导入纯文本文件。
- 支持的文本格式至少包括：
  - `.txt`
  - `.md`
  - `.markdown`
- 支持 Word 文档：
  - `.docx`
  - `.doc`
- 文件选择后应显示文件名、格式、大小、提取状态和提取字数。
- 用户可以删除已导入文件并重新选择，不允许多个文件内容被意外拼接。

### 2.2 文件提取完整性

文件提取必须遵循“完整成功或明确失败”，禁止在部分内容缺失时仍显示为成功。

- TXT/Markdown：按文件原始顺序读取完整文本，不将 Markdown 渲染为 HTML，不主动删除 Markdown 内容；后续统一由标点处理规则处理标记符号。
- 文本编码至少覆盖 UTF-8、UTF-8 BOM、UTF-16 LE/BE；对常见中文编码应进行可靠识别或给出明确的编码选择入口。
- Word：按阅读顺序提取所有可读取文本，包括：
  - 标题与普通段落；
  - 列表；
  - 表格单元格；
  - 文本框；
  - 超链接的显示文字；
  - 页眉、页脚；
  - 脚注、尾注；
  - 文档中其他可读取文本层。
- 不执行 Word 宏，不访问文档内的外部链接或远程资源。
- 加密、损坏、格式不受支持或只能得到部分内容的文件必须返回明确错误，不能静默返回残缺文本。
- 图片、扫描件中的像素文字不属于“可读取文本层”。如果后续需要识别图片文字，应作为单独 OCR 任务实施，不能在本功能中假装已经完整提取。

### 2.3 格式化输出

- 显示完整格式化结果。
- 显示处理前字数、处理后字数、移除的标点数量、结果行数。
- 支持一键复制完整结果。
- 支持导出完整结果文件。
- 支持设置最大字数并生成分段预览。
- 支持通过下拉菜单选择并复制指定分段、复制单个分段、复制全部分段和导出分段文件。
- 导出内容使用 UTF-8 编码，换行统一为 LF；下载文件名必须经过安全规范化。

## 3. 标点与换行规则

### 3.1 标点判定

实现应按 Unicode 字符处理，不得按 UTF-16 单元或字节切割字符。

“标点符号”至少包含 Unicode 标点类别：

- `Pc`：连接标点；
- `Pd`：破折号；
- `Ps`：左括号；
- `Pe`：右括号；
- `Pi`：起始引号；
- `Pf`：结束引号；
- `Po`：其他标点。

应覆盖常见中英文标点，例如：

```text
，。！？；：、…—
“”‘’（）【】《》〈〉「」『』
,.!?;:'"()[]{}<>/\@#%&*_-
```

货币符号、数学符号、Emoji 和普通文字不是标点时不得被误删。若实现增加额外符号清单，必须在测试中逐项说明，不能使用不可审计的宽泛正则表达式删除字符。

### 3.2 换行归一化

处理步骤固定为：

1. 将 `CRLF` 和单独 `CR` 统一为 `LF`。
2. 保留原文已有的每个换行位置。
3. 将每一组连续标点符号替换为一个逻辑换行。
4. 将连续的逻辑换行归一化为一个 `LF`。
5. 原文换行、标点换行或二者相邻时，都只输出一个连续换行。
6. 除标点符号和被折叠的重复换行外，不改变其他字符的内容与顺序。
7. 不自动改写大小写，不做繁简转换，不纠错，不重排段落，不进行 AI 改写。

空白行只含空格或制表符时，应作为重复换行处理，不能产生视觉上的多重空行；普通文本行内部的空格和制表符必须保留。

### 3.3 示例

输入：

```text
第一行

第二行，第三行！！！
第四行。
```

输出：

```text
第一行
第二行
第三行
第四行
```

输入：

```text
你好？！……世界
```

输出：

```text
你好
世界
```

## 4. 字数统计定义

- 字数以格式化后的文本为准。
- 每个 Unicode 文字、英文字符、数字或其他保留字符计为一个字。
- 换行不计入字数。
- 普通空格、制表符和其他纯空白字符保留，但不计入字数。
- 标点已经被移除，因此不计入字数。
- 页面必须在分段前显示采用的计数口径，避免用户误解。

实现必须使用同一个计数函数驱动：

- 总字数；
- 单行字数；
- 分段字数；
- 字数上限校验；
- 导出文件中的分段摘要。

不允许页面显示的数字与实际分段算法使用不同口径。

## 5. 分段算法

### 5.1 输入约束

- 最大字数必须是正整数。
- 空值、零、负数、小数、非数字或超过系统安全上限的值必须拒绝，并给出明确提示。
- 分段输入使用已经完成标点移除和换行归一化的结果。

### 5.2 固定算法

按原顺序逐行处理：

```text
segments = []
currentLines = []
currentCount = 0

for each line:
    lineCount = countCharacters(line)

    if currentLines is empty:
        start current segment with this line
    else if currentCount + lineCount <= limit:
        append the whole line to current segment
    else:
        finish current segment before this line
        start a new segment with the whole line

finish the last non-empty segment
```

关键行为：

- 不对单行进行切片。
- 不把会导致超限的行加入当前分段。
- 加入下一行将超过上限时，分段边界就是该行之前的原有换行。
- 当前分段在上一完整行结束，超限行完整进入下一分段。
- 不为了接近字数上限而重排、重复或丢弃任何行。
- 分段之间不额外添加正文内容；界面标签和字数摘要不得混入用户导出的正文。

### 5.3 示例

最大字数为 `10`，输入：

```text
甲甲甲甲
乙乙乙乙
丙丙丙丙
```

结果：

```text
第 1 段，8 字：
甲甲甲甲
乙乙乙乙

第 2 段，4 字：
丙丙丙丙
```

第三行如果加入第一段会得到 12 字，因此第一段在第二行之后结束，第三行完整成为第二段的开始。

### 5.4 单行本身超限

如果某一整行自身已经超过最大字数，则“不超过上限”和“不在行中截断”无法同时成立。

此时必须：

1. 不截断该行。
2. 不生成声称满足限制的错误结果。
3. 标记具体行号、该行字数和当前上限。
4. 阻止受影响的分段导出，直到用户提高字数上限或修改原文换行。
5. 提示内容应为：“第 N 行共 X 字，超过每段 Y 字限制；请提高字数上限或在原文中增加换行。”

## 6. 页面交互

新增独立的“文本格式化”工具入口，视觉和交互必须复用现有网站的卡片、主题、语言和按钮体系。

建议页面结构：

1. 输入方式切换：粘贴文本 / 导入文件。
2. 原文区域：可编辑、可清空、显示文件信息与提取统计。
3. 主操作：“移除标点并整理换行”。
4. 格式化结果区域：只读预览、完整字数、行数与标点统计。
5. 完整结果操作：一键复制、导出 TXT。
6. 分段设置：最大字数输入框、“生成分段”按钮、分段选择菜单和“复制分段”按钮。
7. 分段结果：显示序号、字数、行数、正文预览、复制按钮。
8. 分段导出：分别导出 `.txt`，或打包为一个 ZIP；文件名使用零填充序号，例如：

```text
文章名-001.txt
文章名-002.txt
文章名-003.txt
```

用户修改原文、重新导入文件或重新执行格式化后，已有分段结果必须失效并重新计算，不能展示旧结果。

## 7. 完整性与隐私

- 优先在浏览器本地完成文本处理与导出，避免文章离开用户设备。
- 如果 Word 提取必须经过服务端：
  - 仅在内存或项目临时目录中处理；
  - 不写入访问日志、错误日志或分析系统；
  - 不调用外部 AI、OCR 或第三方转换服务；
  - 临时文件必须按既有任务生命周期或更短时间删除；
  - 继续受现有嵌入会话门禁、上传大小限制和限流保护。
- 不执行宏、脚本、外部模板、嵌入对象或远程链接。
- 文件类型必须同时校验扩展名、MIME 和文件签名，不能只相信浏览器提供的 MIME。
- 上传大小上限必须明确显示；超过上限时整体拒绝，不能截断文件后继续处理。

## 8. 错误处理

必须区分并明确提示：

- 文件为空；
- 文件编码无法识别；
- 文件格式与扩展名不符；
- Word 文件加密；
- Word 文件损坏；
- 文档包含当前提取器无法完整读取的文本区域；
- 文件超过大小限制；
- 格式化结果为空；
- 字数上限无效；
- 单行字数超过上限；
- 剪贴板权限失败；
- 浏览器下载失败。

任何错误不得清空用户已经输入或成功提取的原文。

## 9. 测试驱动实施顺序

必须先写失败测试，再编写实现。

### 9.1 文本转换单元测试

- 中英文标点全部被移除。
- 中文、英文、数字、Emoji 和非标点符号不被误删。
- 单个标点转换为一个换行。
- 多个连续标点只转换为一个换行。
- 原文单个换行被保留。
- 原文多个连续换行只保留一个换行。
- 标点换行与原文换行相邻时只保留一个换行。
- `CRLF`、`CR`、`LF` 得到一致结果。
- 普通行内空格、制表符和字符顺序保持不变。
- 大文本处理不出现截断或乱码。

### 9.2 分段算法单元测试

- 总字数小于上限时只有一个分段。
- 总字数恰好等于上限时只有一个分段。
- 加入下一行会超限时，在该行之前分段。
- 下一行完整进入下一分段。
- 每个正常分段字数都小于或等于上限。
- 不在行中间截断。
- 空行归一化后不会产生空分段。
- 单行本身超限时返回带行号的明确错误。
- 分段重新拼接后，正文与格式化结果完全一致。
- 分段顺序稳定，不重排、不遗漏、不重复。

### 9.3 文件提取测试

为每种格式建立固定测试文档，并逐字符比较期望文本：

- UTF-8 TXT；
- UTF-8 BOM TXT；
- UTF-16 TXT；
- 中文编码 TXT；
- Markdown 标题、列表、代码块、链接和表格；
- DOCX 标题、段落、列表、表格、文本框、页眉页脚和脚注；
- 旧版 DOC 等价样本；
- 加密文档；
- 损坏文档；
- 空文档；
- 大文件边界。

如果任一支持格式只能提取部分内容，该格式不得标记为已支持。

### 9.4 前端测试

- 粘贴文本、导入、清空和重新导入。
- 文件类型和大小校验。
- 原文、格式化结果和统计值同步。
- 一键复制成功与失败提示。
- 完整结果导出内容正确。
- 字数输入校验和超长行提示。
- 分段卡片顺序、字数和内容正确。
- 分段文件名、编码和内容正确。
- 深色/浅色主题和中英文界面不回归。
- 桌面端与 390px 宽度无横向溢出。

## 10. 验收示例

### FT-01 原换行与标点换行合并

输入：

```text
第一行

第二行，第三行！！！
第四行。
```

期望：

```text
第一行
第二行
第三行
第四行
```

### FT-02 连续标点

输入：

```text
你好？！……世界
```

期望：

```text
你好
世界
```

### FT-03 下一行导致超限

上限：`10`

输入：

```text
甲甲甲甲
乙乙乙乙
丙丙丙丙
```

期望：

- 第 1 段：前两行，共 8 字。
- 第 2 段：第三行，共 4 字。
- 两段均不超过 10 字。
- 没有任何一行被截断。

### FT-04 单行本身超限

上限：`5`

输入：

```text
这一整行有十个文字
```

期望：

- 不截断该行。
- 不生成错误的“合规分段”。
- 显示该行行号、实际字数和上限，并要求用户提高上限或增加原文换行。

### FT-05 完整性不变量

对任何成功处理的输入：

- 移除原文中的标点与重复换行后，保留字符序列必须与格式化结果一致。
- 将所有分段按原分隔换行重新拼接，必须与完整格式化结果逐字符一致。
- 导出的文本必须与页面预览逐字符一致。

## 11. 明确不做

- 不进行 AI 改写、摘要、润色、纠错或续写。
- 不做繁简转换、大小写转换或词语替换。
- 不自动添加任何标点。
- 不自动改变用户句子顺序。
- 不在行中间强制截断。
- 不静默忽略无法提取的文档区域。
- 不执行 Word 宏或嵌入脚本。
- 不对图片文字进行隐式 OCR。
- 不将文章内容发送给外部服务。

## 12. 完成定义

只有同时满足以下条件，任务才能标记为完成：

- [x] 输入、文件导入、格式化、复制、完整导出和分段导出全部实现。
- [x] 所有声明支持的文件格式通过逐字符提取测试。
- [x] 标点移除后没有任何非标点文字缺失、重复或乱序。
- [x] 原文换行保留，连续换行最终只有一个。
- [x] 所有正常分段均不超过用户设定字数。
- [x] 分段只发生在换行边界，没有完整行被截断。
- [x] 下一行会超限时，当前分段在该行之前结束。
- [x] 单行超限时阻止错误导出并显示明确提示。
- [x] 分段重新拼接与完整格式化结果逐字符一致。
- [x] 导出文件与页面预览逐字符一致。
- [x] Go/前端单元测试、类型检查、构建和必要的端到端测试通过。
- [x] 深色/浅色主题、主站嵌入和现有媒体工具没有回归。
- [x] 未修改主站项目；仅在当前 Video Collector 项目内实施。

## 13. 实施进度与验收记录

进度：核心功能与本次界面增强均为 `100%（实现、发布与生产验收完成）`。正式站点 `https://video-collector.ximoai.cn` 已运行不可变镜像 `ghcr.io/chenlinning/video-collector:sha-ff39ec1`。

### 13.1 已实施范围

- 新增独立“文本格式化”工具入口，复用现有卡片、按钮、主题、语言和主站偏好同步协议。
- 浏览器本地完成 Unicode 标点替换、换行归一化、统一字数统计、按完整行分段、复制和 UTF-8 TXT 导出。
- 新增同源 `POST /api/v1/text/extract`，单文件上限 20 MiB，并继续受现有嵌入门禁与任务限流保护。
- TXT/Markdown 支持 UTF-8、UTF-8 BOM、UTF-16 LE/BE 和 GB18030；Markdown 按源码原样提取。
- DOCX 在内存中解析 OpenXML，覆盖正文、超链接显示文字、表格、文本框、页眉页脚、脚注、尾注和批注；拒绝加密、损坏、非 DOCX ZIP、重复替代分支和格式/MIME/签名不匹配。
- 旧版 DOC 使用容器内离线 `antiword`；临时文件权限为 `0600`，调用结束后删除，不访问外部转换服务。
- 单行超过限制、空结果、无效限制、提取失败、剪贴板失败和下载创建失败均提供明确提示；失败不会清空已成功输入或提取的原文。

### 13.2 自动化验收（2026-07-28）

| 项目 | 结果 | 记录 |
|---|---|---|
| 前端测试 | 通过 | `npm test -- --run`：13 个测试文件、66 项测试全部通过；其中文本规则与导出核心 14 项。 |
| 类型检查 | 通过 | `npm run typecheck`。 |
| Web 构建 | 通过 | `npm run build:web`，36 个模块完成构建。 |
| Electron 构建 | 通过 | `npm run build`，主进程、preload 和 renderer 均成功，`PRELOAD_BUILD_OK`。 |
| Go 测试 | 通过 | `go test ./server/...`；`textformatter` 12 项测试覆盖编码、Markdown、DOCX、DOC、空文件、加密、损坏和类型不匹配。 |
| Go 静态检查 | 通过 | `go vet ./server/...`。 |
| 上传边界 | 通过 | multipart 文本提取和超过 20 MiB 返回 413 的服务端测试通过。 |
| Compose | 通过 | `docker compose config --quiet`。 |
| 完整镜像 | 通过 | `video-collector:text-formatter-final`（镜像 ID `sha256:5b8ac2c78b77a92ce3b0dcff6f11975ae0026a9197c59e82396f4b5ec8a17e3f`）构建成功；`/health` 返回 `status=ok`，镜像内 `antiword` 位于 `/usr/bin/antiword`。 |
| 旧版 DOC 实样 | 通过 | Apache POI `simple.doc`，SHA-256 `4876e828e62d490fa188c7c4e47013e2e0dfdf02f5903dfd374bc0d2b2049ca1`；HTTP 提取结果与镜像内 antiword 输出逐 UTF-8 字节一致，共 49 个字符。 |

### 13.3 浏览器验收（本地最终功能）

- FT-01：输入原换行与连续标点样本后，结果统计为原文 17 字、结果 12 字、移除 5 个标点；重复换行合并正确。
- FT-03：上限 10 时生成两段，分别为 8 字/2 行和 4 字/1 行；全部分段复制后剪贴板内容与完整格式化结果逐字符一致。
- FT-04：单行 9 字、上限 5 时显示准确行号与字数，不生成分段卡片，分段导出保持禁用。
- Markdown 文件真实导入显示文件名、MD 格式、14.1 KiB、提取成功和 6,329 个提取字符；随后导入不支持文件失败时，原文 6,329 个字符保持不变。
- 仅含标点的输入会显示“格式化结果为空”，完整导出保持禁用。
- 导出 Blob 的 MIME 为 `text/plain;charset=utf-8`，其 UTF-8 字节和页面预览逐字符一致；完整文件名与分段文件名均通过安全规范化，分段序号使用三位零填充。
- 390px 深色与浅色主题实测 `scrollWidth === clientWidth === 375`，文本工具宽 355px、分段控件宽 319px，无横向溢出；主题变量分别为 `dark` 和 `light`。
- 既有主题/语言桥、嵌入门禁、媒体解析与任务测试均包含在 66 项前端测试和 Go 全模块回归中，没有修改主站代码。

### 13.4 生产发布状态

- 功能提交：`20d2cfe`；GitHub Actions：`30356002416`；不可变镜像：`ghcr.io/chenlinning/video-collector:sha-20d2cfe`；OCI 摘要：`sha256:b9394c45cff8bb1beda090f438dab72a88c398aa8000def15e2673d7529e16a8`。
- 2026-07-28 已部署至唯一生产服务器 `47.251.87.147`。部署前镜像为 `sha-c666bcc`；最终 `.env` 备份为 `/opt/video-collector/.env.bak.text-20260728-202446`，旧镜像回滚标签为 `video-collector:rollback-pre-text-20260728-202446`。
- 保留生产 `docker-compose.yml` 的 CPU、内存、Swap 和 OOM 专用限制，仅修改 `.env` 中的 `VIDEO_COLLECTOR_IMAGE`；没有覆盖生产本地配置，也没有修改主站项目。
- 生产容器最终为 `running/healthy/0`，`/health` 返回 `status=ok`、`egressStatus=available`；`antiword` 位于 `/usr/bin/antiword`；默认路由与 `wg-quick@wg-vc-egress.service=active` 均保持不变。
- 首次切换后容器已经健康，但验收命令错误地把 Shell 内建 `command` 直接交给 `docker exec`，自动回滚成功；确认目标镜像实际包含 `antiword` 后修正为 `sh -lc` 并重新发布成功。该过程同时验证了 `.env` 与旧镜像回滚路径可用。
- 从 `https://ximoai.cn/home` 的“视频收藏”承载页进入后，第六个“文本格式化”入口正常显示。线上样例原文 24 字，经处理得到 19 字、移除 5 个标点、5 行；上限 10 时生成 10 字/3 行和 9 字/2 行两段；15 字单行在上限 10 时保持完整并显示准确超限提示；“复制全部”显示“已复制完整结果”。
- 无 Cookie 的匿名根页面请求返回 HTTP 403；已从主站取得短期软门禁会话的浏览器可在 TTL 内复用授权 Cookie，这与 `TASK-EMBED-ACCESS-GATE.md` 的设计一致。
- 生产页面已确认展示 TXT、MD、Markdown、DOCX、DOC 文件入口和 20 MiB 限制；CI、本地浏览器真实 Markdown 导入、服务端各格式提取测试及生产 `antiword` 运行依赖均通过。本次生产浏览器的本地测试文件选择器被 ChatGPT Chrome Extension 的“Allow access to file URLs”权限阻止，因此没有把生产 TXT/Markdown 选择器自动化标记为已执行；这是验收环境限制，不是站点上传错误。

### 13.5 紧凑工具栏与分段选择复制增强（2026-07-28，本地）

- 桌面端 6 个工具入口改为同一行，实测 1265px 内容宽度下为 6 列，每项宽约 198px、高 56px，顶部位置完全一致；800px 和 560px 断点仍分别使用 3 列和 2 列。
- “生成分段”后新增分段选择菜单，选项包含段号、字数和行数；生成成功默认选中第 1 段，无有效分段时菜单与“复制分段”按钮禁用。
- “复制全部分段”前新增“复制分段”；浏览器实测切换到第 2 段后，剪贴板内容仅为第 2 段正文，并显示“第 2 段已复制”。
- 修改原文、重新格式化、清空、调整字数上限或分段失败时会清空旧分段选择，避免复制过期结果；中英文文案同步完成。
- 390px 视口实测 `scrollWidth === clientWidth === 375`，分段控件和下拉菜单均为 319px，无横向溢出；900px 中等桌面宽度下分段控件自然换行且无溢出。
- `npm test -- --run`：13 个测试文件、69 项测试全部通过；`npm run typecheck`、`npm run build:web` 和 `git diff --check` 通过。
- 功能提交：`ff39ec1`；GitHub Actions：`30364654192`；不可变镜像：`ghcr.io/chenlinning/video-collector:sha-ff39ec1`；OCI 摘要：`sha256:f81feab035f78b3acfa4e2bab634b9b968880a6becb79852db7f2b3063e7f151`。
- 已部署到唯一生产服务器 `47.251.87.147`；部署前镜像为 `sha-20d2cfe`，`.env` 备份为 `/opt/video-collector/.env.bak.segment-ui-20260728-214727`，回滚标签为 `video-collector:rollback-pre-segment-ui-20260728-214727`。
- 生产容器最终为 `running/healthy/0`，健康接口仍为 `status=ok`、`egressStatus=available`，`antiword=/usr/bin/antiword`；默认路由与 `wg-quick@wg-vc-egress.service=active` 保持不变。
- 正式站点桌面布局实测 6 个工具入口处于同一行且高度均为 56px；390px 视口下 `scrollWidth === clientWidth === 375`，工具栏为 2 列、3 行，分段菜单宽 319px，无横向溢出。
- 正式站点生成两段后默认选择第 1 段，菜单显示“第 1 段 · 5 字 · 2 行”和“第 2 段 · 3 字 · 1 行”；切换到第 2 段后，“复制分段”只写入 `丙丙丙`，并显示“第 2 段已复制”。
- 本次增强未修改主站项目；生产验收通过主站承载页建立的短期授权会话完成。
